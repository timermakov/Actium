# Placeholder — apply real values on deploy:
# kubectl create secret generic postgres-credentials -n actium-database --from-env-file=.env.local ...
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: actium-database
type: Opaque
stringData:
  DB_USER: postgres
  DB_PASSWORD: changeme
  DB_NAME: docflow
