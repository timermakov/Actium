# Placeholder — override on deploy via:
# kubectl create secret generic backend-secrets -n actium-backend --from-env-file=.env.local --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: actium-backend
type: Opaque
stringData:
  DB_PASSWORD: changeme
  JWT_SECRET: changeme
